// BoilVerdict 컴포넌트 정의
// props: 부모 컴포넌트로부터 전달받은 데이터 객체
function BoilVerdict(props) {
    // props 객체 안의 celsius(섭씨 온도) 값이 100 이상인지 확인
    if (props.celsius >= 100) {
        // 온도가 100도 이상일 경우 실행되는 결과
        return <p>물이 끓습니다.</p>;
    }

    // 온도가 100도 미만일 경우 실행되는 결과 (기본 반환값)
    return <p>물이 끓지 않습니다.</p>;
}

// r1
// BoilingVerdict 컴포넌트정의
// props:부모컴포넌트로부터 전달받은 데이터 객체
function BoilVerdict(prpos) {
    // props 객체 안의 celsius(섭씨온도)값이 100이상인지 확인
    if (props.celsius >= 100) {
        // 온도가 100도 이상인 경우 실행되는 결과 
        return <p>물이 끓습니다.</p>;
    }
    // 온도가 100도 미만일 경우 실행되는 결과(기본반환값)
    return <p>물이 끓지 않습니다.</p>;
}

// r2
// BoilngVerdict 컴포넌트정의
// props:부모컴포넌트로부터 전달받은 데이터 객체
function BoilVerdict(props) {
    // props 객체 안의 celsius(섭씨온도)값이 100이상인지 확인
    if (props.celsius >= 100) {
        // 온도가 100도 이상인 경우 실행되는 결과
        return <p>물이 끓습니다.</p>;
    }
    // 온도가 100도 미만일 경우 실행되는 결과(기본 반환값)
    return <p>물이 끓지 않습니다.</p>;
}

// r3
// BoilVerdict 컴포넌트 정의
// props: 부모 컴포넌트로부터 전달받은 데이터 객체
function BoilingVerdict(props) {
    // props 객체 안의 celsius(섭씨 온도)값이 100 이상인지 확인
    if (props.celsius >= 100) {
        // 온도가 100도 이상일 경우 실행되는 결과
        return <p>물이 끓습니다.</p>;
    }
    // 온도가 100도 미만일 경우 실행되는 결과(기본 반환값)
    return <p>물이 끓지 않습니다.</p>;
}

// r4
// BoilingVerdict 컴포넌트 정의
// props: 부모 컴포넌트로부터 전달받은 데이터 객체
function BoilingVerdict(props) {
    // props 객체 안의 celsius(섭씨 온도)값이 100 이상인지 확인
    if (props.celsius >= 100) {
        // 온도가 100도 이사일 경우 실행되는 결과
        return <p>물이 끓습니다.</p>;
    }
    // 온도가 100도 미만일 경우 실행되는 결과(기본 반환값)
    return <p>물이 끓지 않습니다.</p>;
}

// r5
// BoilingVerdict 컴포넌트 정의
// props: 부모 컴포넌트로부터 전달받은 데이터 객체
function BoilingVerdict(props) {
    // props 객체 안의 celsius(섭씨 온도)값이 100 이상인지 확인
    if (props.celsius >= 100) {
        // 온도가 100도 이상일 경우 실행되는 결과
        return <p>물이 끓습니다.</p>;
    }
    // 온도가 100도 미만일 경우 실행되는 결과(기본 반환값)
    return <p>물이 끓지 않습니다.</p>;
}
