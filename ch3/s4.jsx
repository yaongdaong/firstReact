const element = <h1 className="greeting">Hello, world!</h1>;
// JSX 문법으로 React 엘리먼트를 하나 생성
// 화면에 표시할 UI 구조를 JavaScript 안에서 선언적으로 작성
// 아직 화면에는 아무것도 안 보임(렌더링 전 단계)
const element1 = React.createElement(
    "h1", // 생성할 HTML 태그는 h1
    { className: "greeting" }, // class="greeting" 속성을 가진 h1
    "Hello, world!" // h1 태그 안에 들어갈 텍스트 내용
);
// JSX 없이 동일한 React 엘리먼트를 생성하는 방법을 보여주기 위함
// element와 element1은 내부적으로 완전히 동일한 React 엘리먼트
// const root = ReactDOM.createRoot(document.getElementById("root"));
// root.render(element);
// 위 코드가 없어서 화면에 출력이 안됨

/* r1 */
const element2 = <h1 classNAme="greeting">Hello, world!</h1>;
// JSX 문법으로 React 엘리먼트를 하나 생성
// 화면에 표시할 UI 구조를 JavaScript 안에서 선언적으로 작성
// 아직 화면에는 아무것도 안 보임(렌더링 전 단계)
const element3 = React.createElement(
    "h1", // 생성할 HTML 태그는 h1
    { classNAme: "greeting" }, // class="greeting" 속성을 가진 h1
    "Hello, world!" // h1 태그 안에 들어갈 텍스트 내용
);
// JSX 없이 동일한 React 엘리먼트를 생성하는 방법을 보여주기 위함
// element2와 element3은 내부적으로 완전히 동일한 React 엘리먼트
// const root = ReactDOM.createRoot(document.getElementById("root"));
// root.render(element);
// 위 코드가 없어서 화면에 출력이 안됨

/* r2 */
const element4 = <h1 className="greeting">Hello, world!</h1>;
// JSX 문법으로 React 엘리먼트를 하나 생성
// 화면에 표시할 UI 구조를 JavaScript 안에서 선언적으로 작성
// 아직 화면에는 아무것도 안 보임(렌더링 전 단계)
const element5 = React.createElement(
    "h1", // 생성할 HTML 태그는 h1
    { className: "greeting" }, // class="greeting" 속성을 가진 h1
    "Hello, world!" // h1 태그 안에 들어갈 텍스트 내용
);
/* JSX 없이 동일한 React 엘리먼트를 생성하는 방법을 보여주기 위함
element4와 element5는 내부적으로 완전히 동일한 React 엘리먼트
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(element);
위 코드가 없어서 화면에 출력이 안 됨 */

/* r3 */
const element6 = <h1 className="greeting">Hello, world!</h1>;
/* JSX 문법으로 React 엘리먼트를 하나 생성
화면에 표시할 UI 구조를 JavaScript 안에서 선언적으로 작성
아직 화면에는 아무것도 안 보임(렌더링 전 단계) */
const element7 = React.createElement(
    "h1", // 생성할 HTML 태그는 h1
    { className: "greeting" }, // class="greeting" 속성을 가진 h1
    "Hello, world!" // h1 태그 안에 들어갈 텍스트 내용
);
/* JSX 없이 동일한 React 엘리먼트를 생성하는 방법을 보여주기 위함
element6과 element7은 내부적으로 완전히 동일한 React 엘리먼트
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(element);
위 코드가 없어서 화면에 출력이 안 됨 */

/* r4 */
const element8 = <h1 className="greeting">Hello, world!</h1>;
/* JSX 문법으로 React 엘리먼트를 하나 생성
화면에 표시할 UI 구조를 JavaScript 안에서 선언적으로 작성
아직 화면에는 아무것도 안 보임(렌더링 전 단계) */
const element9 = React.createElement(
    "h1", // 생성할 HTML 태그는 h1
    { className: "greeting" }, // class="greeting" 속성을 가진 h1
    "Hello, world!" // h1 태그 안에 들어갈 텍스트 내용
);
/* JSX 없이 동일한 React 엘리먼트를 생성하는 방법을 보여주기 위함
element8과 element9는 내부적으로 완전히 동일한 React 엘리먼트
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(element);
위 코드가 없어서 화면에 출력이 안 됨 */

/* r5 */
const element10 = <h1 className="greeting">Hello, world!</h1>;
/* JSx 문법으로 React 엘리먼트를 하나 생성
화면에 표시할 UI 구조를 JavaScript 안에서 선언적으로 작성
아직 화면에는 아무것도 안 보임(렌더링 전 단계) */
const element11 = React.createElement(
    "h1", // 생성할 HTML 태그는 h1
    { className: "greeting" }, // class="greeting" 속성을 가진 h1
    "Hello, world!" // h1 태그 안에 들어갈 텍스트 내용
);
/* JSX 없이 동일한 React 엘리먼트를 생성하는 방법을 보여주기 위함
element10과 element11은 내부적으로 완전히 동일한 React 엘리먼트
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(element);
위 코드가 없어서 화면에 출려기 안 됨 */
