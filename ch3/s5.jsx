// createElement() 함수의 호출 결과로 나온 자바스크립트 객체
const element = {
    // React 엘리먼트를 흉내 낸 일반 JavaScript 객체
    type: "h1",
    // 실제 만들어질 HTML 태그의 종류를 의미 (h1 태그)
    props: {
        // 해당 엘리먼트에 전달되는 속성(props) 객체
        className: "greeting",
        // h1 태그에 적용될 class 속성 (HTML의 class)
        children: "Hello, world!",
        // h1 태그 안에 들어갈 자식 내용(텍스트 또는 다른 엘리먼트)
    },
};

/* r1 */
// createElement() 함수의 호출 결과로 나온 자바스크립트 객체
const element1 = {
    // React 엘리먼트를 흉내 낸 일반 JavaScript 객체
    type: "h1", // 실제 만들어진 HTML 태그의 종류를 의미(h1 태그)
    props: {
        // 해당 엘리먼트에 전달되는 속성(props) 객체
        className: "greeting", // h1 태그에 적용될 class 속성(HTML의 class)
        children: "Hello, world!", // h1 태그 안에 들어갈 자식 내용(텍스트 또는 다른 엘리먼트)
    },
};

/* r2 */
// createElement() 함수의 호출 결과로 나온 자바스크립트 객체
const element2 = {
    // React 엘리먼트를 흉내 낸 일반 JavaScript 객체
    type: "h1", // 실제 만들어진 HTML 태그의 종류를 의미(h1 태그)
    props: {
        // 해당 엘리먼트에 전달되는 속성(props) 객체
        className: "greeting", // h1 태그에 적용될 class 속성(HTML의 class)
        children: "Hello, world!", // h1 태그 안에 들어갈 자식 내용(텍스트 또는 다른 엘리먼트)
    },
};

/* r3 */
// createElement() 함수의 호출 결과로 나온 자바스크립트 객체
const element3 = {
    // React 엘리먼트를 흉내 낸 일반 JavaScript 객체
    type: "h1", // 실제 만들어진 HTML 태그의 종류를 의미(h1 태그)
    props: {
        // 해당 엘리먼트에 전달되는 속성(props) 객체
        className: "greeting", // h1 태그에 적용될 class 속성(HTML의 class)
        children: "Hello, world!", // h1 태그 안에 들어갈 자식 내용(텍스트 또는 다른 엘리먼트)
    },
};

/* r4 */
// createElement() 함수의 호출 결과로 나온 자바스크립트 객체
const element4 = {
    // React 엘리먼트를 흉내 낸 일반 JavaScript 객체
    type: "h1", // 실제 만들어진 HTMl 태그의 종류를 의미(h1 태그)
    props: {
        // 해당 엘리먼트에 전달되는 속성(props) 객체
        className: "greeting", // h1 태그에 적용될 class 속성(HTML의 class)
        children: "Hello, world!", // h1 태그 안에 들어갈 자식 내용(텍스트 또는 다른 엘리먼트)
    },
};

/* r5 */
// createEleemnt() 함수의 호출 결과로 나온 자바스크립트 객체
const element5 = {
    // React 엘리먼트를 흉내 낸 일반 JavaScript 객체
    type: "h1", // 실제 만들어진 HTMl 태그의 종류를 의미(h1 태그)
    props: {
        // 해당 엘리먼트에 전달되는 속성(props) 객체
        className: "greeting", // h1 태그에 적용될 class 속성(HTMl의 class)
        children: "Hello, world!", // h1 태그 안에 들어갈 자식 내용(텍스트 또는 다른 엘리먼트)
    },
};
