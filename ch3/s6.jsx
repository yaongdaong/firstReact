// createElement() 함수의 파라미터
React.createElement(
    // React 엘리먼트 객체를 생성하는 함수
    type,
    /* 생성할 엘리먼트의 타입
  문자열이면 HTML 태그
  함수/클래스면 React 컴포넌트 */
    [props],
    /* 해당 엘리먼트에 전달할 속성 객체
  className, id, style, onClick 같은 값들
  없으면 null 또는 생략 가능 */
    [...children]
    /* 엘리먼트 안에 들어갈 자식들
  문자열(텍스트), 다른 React 엘리먼트, 또는 여러 개 가능 */
);

/* r1 */
// createElement() 함수의 파라미터
React.createElement(
    // React 엘리먼트 객체를 생성하는 함수
    type,
    /* 생성할 엘리먼트의 타입, 문자열이면 HTML 태그, 함수/클래스면 React 컴포넌트 */
    [props],
    /* 해당 엘리먼트에 전달할 속성 객체, className, id, style, onClick 같은 값들, 없으면 null 또는 생략 가능 */
    [...children]
    /* 엘리먼트 안에 들어갈 자식들, 문자열(텍스트), 다른 React 엘리먼트, 또는 여러 개 가능 */
);

/* r2 */
// createElement() 함수의 파라미터
React.createElement(
    // React 엘리먼트 객체를 생성하는 함수
    type,
    /* 생성할 엘리먼트의 타입, 문자열이면 HTML 태그, 함수/클래스면 React 컴포넌트 */
    [props],
    /* 해당 엘리먼트에 전달할 속성 객체, className, id, style, onClick 같은 값들, 없으면 null 또는 생략 가능 */
    [...children]
    /* 엘리먼트 안에 들어갈 자식들, 문자열(텍스트), 다른 React 엘리먼트, 또는 여러 개 가능 */
);

/* r3 */
// createElement() 함수의 파라미터
React.createElement(
    // React 엘리먼트 객체를 생성하는 함수
    type,
    /* 생성할 엘리먼트의 타입, 문자열이면 HTML 태그, 함수/클래스면 React 컴포넌트 */
    [props],
    /* 해당 엘리먼트에 전달할 속성 객체, className, id, style, onClick 같은 값들, 없으면 null 또는 생략 가능 */
    [...children]
    /* 엘리먼트 안에 들어갈 자식들, 문자열(텍스트), 다른 React 엘리먼트, 또는 여러 개 가능 */
);

/* r4 */
// createElement() 함수의 파라미터
React.createElement(
    // React 엘리먼트 객체를 생성하는 함수
    type,
    /* 생성할 엘리먼트의 타입, 문자열이면 HTML 태그, 함수/클래스면 React 컴포넌트 */
    [props],
    /* 해당 엘리먼트에 전달할 속성 객체, className, id, style, onclick 같은 값들, 없으면 null 또는 생략 가능 */
    [...children]
    /* 엘리먼트 안에 들어갈 자식들, 문자열(텍스트), 다른 React 엘리먼트, 또는 여러 개 가능
     */
);

/* r5 */
// createElement() 함수의 파라미터
React.createElement(
    // React 엘리먼트 객체를 생성하는 함수
    type,
    /* 생성할 엘리먼트의 타입, 문자열이면 HTML 태그, 함수/클래스면 React 컴포넌트 */
    [props],
    /* 해당 엘리먼트에 전달할 속성 객체, className, id, style, onClick 같은 값들, 없으면 null 또는 생략 가능 */
    [...children]
    /* 엘리먼트 안에 들어갈 자식들, 문자열(텍스트), 다른 React 엘리먼트, 또는 여러 개 가능 */
);
