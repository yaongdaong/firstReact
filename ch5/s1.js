// 리액트 컴포넌트의 props는 바꿀 수 없고, 같은 props가 들어오면 항상 같은 엘리먼트를 리턴해야 한다
// pure 함수
// input을 변경하지 않으며 같은 input에 대해서 항상 같은 output을 리턴
function sum(a, b) {
    return a + b;
}
// impure 함수
// input을 변경함
function withdraw(account, amount) {
    account.total -= amount;
}

// r1
// 리액트 컴포넌트의 props는 바꿀 수 없고, 같은 props가 들어오면 항상 같은 엘리먼트를 리턴해야 한다.
// pure 함수
// input을 변경하지 않으며 같은 input에 대하여 항상 같은 output을 리턴
function sum(a, b) {
    return a + b;
}
// impure 함수
// input을 변경함
function withdraw(account, amount) {
    account.total -= amount;
}

// r2
// 리액트 컴포넌트의 props는 바꿀 수 없고, 같은 props가 들어오면 항상 같은 엘리먼트를 리턴해야 한다.
// pure 함수
// input을 변경하지 않으며 같은 input에 대하여 항상 같은 output을 리턴
function sum(a, b) {
    return a + b;
}
// impure 함수
// input을 변경함
function withdraw(account, amount) {
    account.total -= amount;
}

// r3
// 리액트 컴포넌트의 props는 바꿀 수 없고, 같은 props가 들어오면 항상 같은 엘리먼트를 리턴해야 한다.
// pure 함수
// input을 변경하지 않으며 같은 input에 대하여 항상 같은 output을 리턴
function sum(a, b) {
    return a + b;
}
// impure 함수
// input을 변경함
function withdraw(account, amount) {
    account.total -= amount;
}

// r4
// 리액트 컴포넌트의 props는 바꿀 수 없고, 같은 props가 들어오면 항상 같은 엘리먼트를 리턴해야 한다.
// pure 함수
// input을 변경하지 않으며 같은 input에 대하여 항상 같은 output을 리턴
function sum(a, b) {
    return a + b;
}
// impure 함수
// input을 변경함
function withdraw(account, amount) {
    account.total -= amount;
}

// r5
// 리액트 컴포넌트의 props는 바꿀 수 없고, 같은 props가 들어오면 항상 같은 엘리먼트를 리턴해야 한다.
// pure 함수
// input을 변경하지 않으며 같은 input에 대하여 항상 같은 output을 리턴
function sum(a, b) {
    return a + b;
}
// impure 함수
// input을 변경함
function withdraw(account, amount) {
    account.total -= amount;
}
