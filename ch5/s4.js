// JSX를 사용하지 않고 React 엘리먼트를 생성하는 방법
React.createElement(
    Profile, // 1️⃣ 생성할 컴포넌트 (또는 태그 이름)
    {
        // 2️⃣ 두 번째 인자: props 객체
        name: "소플", // Profile 컴포넌트로 전달될 name props
        introduction: "안녕하세요, 소플입니다.", // 소개글 props
        viewCount: 1500, // 숫자 타입 props (조회수)
    },
    null // 3️⃣ children 값 (자식 요소가 없으므로 null)
);

// r1
// JSX를 사용하지 않고 React 엘리먼트를 생성하는 방법
React.createElement(
    Profile, // 1. 생성할 컴포넌트(또는 태그 이름)
    { name: "소플", introduction: "안녕하세요, 소플입니다.", viewCount: 1500 }, // 2. props 객체
    null // 3. children값 (자식 요소가 없으므로 null)
);

// r2
// JSX를 사용하지 않고 React 엘리먼트를 생성하는 방법
React.createElement(
    Profile, // 1. 생성할 컴포넌트(또는 태그 이름)
    { name: "소플", introduction: "안녕하세요, 소플입니다.", viewCount: 1500 }, // 2. props 객체
    null // 3. children값 (자식 요소가 없으므로 null)
);

// r3
// JSX를 사용하지 않고 React 엘리먼트를 생성하는 방법
React.createElement(
    Profile, // 1. 생성할 컴포넌트(또는 태그 이름)
    { name: "소플", introduction: "안녕하세요, 소플입니다.", viewCount: 1500 }, // 2. props객체
    null // 3. children값 (자식 요소가 없으므로 null)
);

// r4
// JSX를 사용하지 않고 React 엘리먼트를 생성하는 압법
React.createElement(
    Profile, // 1. 생성할 컴포넌트(또는 태그 이름)
    { name: "소플", introduction: "안녕하세요, 소플입니다.", viewCount: 1500 }, // 2. props 객체
    null // 3. children값 (자식 요소가 없으므로 null)
);

// r5
// JSX를 사용하지 않고 React 엘리먼트를 생성하는 방법
React.createElement(
    Profile, // 1. 생성할 컴포넌트(또는 태그 이름)
    { name: "소플", introduction: "안녕하세요, 소플입니다.", viewCount: 1500 }, // 2. props 객ㅊ
    null // 3. children값 (자식 요소가 없으므로 null)
);
