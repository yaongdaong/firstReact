// * state: 리액트 컴포넌트에서 값이 변경될 수 있는 데이터 저장소
// * 값이 변경되면 화면이 자동으로 다시 렌더링됨
// * 자바스크립트 객체 형태로 여러 상태를 관리함
// 좋아요 버튼 컴포넌트 정의 (클래스형 컴포넌트)
class LikeButton extends React.Component {
    // 생성자: 컴포넌트가 처음 생성될 때 실행됨
    constructor(props) {
        // 부모 클래스(React.Component)의 생성자를 호출
        super(props);
        // 컴포넌트의 상태(state) 초기값 설정
        this.state = {
            liked: false, // 좋아요가 눌리지 않은 상태
        };
    }
}

// r1
// * state: 리액트 컴포넌트에서 값이 변경될 수 있는 데이터 저장소
// * 값이 변경되면 화면에 자동으로 다시 렌더링됨
// * 자바스크립트 객체 형태로 여러 상태를 관리함
// 좋아요 버튼 컴포넌트 정의 (클래스형 컴포넌트)
class LikeButton extends React.Component {
    // 생성자: 컴포넌트가 처음 생성될 떄 실행됨
    constructor(props) {
        // 부모 클래스(React.Component)의 생성자를 호출
        super(props);
        // 컴포넌트의 상태(state) 초기값 설정
        this.state = {
            liked: false, // 좋아요가 눌리지 않은 상태
        };
    }
}
// r2
// * state: 리액트 컴포넌트에서 값이 변경될 수 있는 데이터 저장소
// * 값이 변경되면 화면에 자동으로 다시 렌더링됨
// * 자바스크립트 객체 형태로 여러 상태를 관리함
// 좋아요 버튼 컴포넌트 정의 (클래스형 컴포넌트)
class LikeButton extends React.Component {
    // 생성자: 컴포넌트가 처음 생성될 떄 실행됨
    constructor(props) {
        // 부모 클래스(React.Component)의 생성자를 호출
        super(props);
        // 컴포넌트의 상태(state) 초기값 생성
        this.state = {
            liked: false, // 좋아요가 눌리지 않은 상태
        };
    }
}
// r3
// * state: 리액트 컴포넌트에서 값이 변경될 수 있는 데이터 저장소
// * 값이 변경되면 화면에 자동으로 다시 렌더링됨
// * 자바스크립트 객체 형태로 여러 상태를 관리함
// 좋아요 버튼 컴포넌트 정의 (클래스형 컴포넌트)
class LikeButton extends React.Component {
    // 생성자: 컴포넌트가 처음 생성될 떄 실행됨
    constructor(props) {
        // 부모 클래스(React.Component)의 생성자를 호출
        super(props);
        // 컴포넌트의 상태(state) 초기값 생성
        this.state = {
            liked: false, // 좋아요가 눌리지 않은 상태
        };
    }
}
// r4
// * state: 리액트 컴포넌트에서 값이 변경될 수 있는 데이터 저장소
// * 값이 변경되면 화면에 자동으로 다시 렌더링
// * 자바스크립트 객체 형태로 여러 상태를 관리함
// 좋아요 버튼 컴포넌트 정의 (클래스형 컴포넌트)
class LikeButton extends React.Component {
    // 생성자: 컴포넌트가 처음 생성될 떄 실행됨
    constructor(props) {
        // 부모 클래스(React.Component)의 생성자를 호출
        super(props);
        // 컴포넌트의 상태(state) 초기값 생성
        this.state = {
            liked: false, // 좋아요가 눌리지 않은 상태
        };
    }
}
// r5
// * state: 리액트 컴포넌트에서 값이 변경될 수 있는 데이터 저장소
// * 값이 변경되면 화면에 자동으로 다시 렌더링
// * 자바스크립트 객체 형태로 여러 상태를 관리함
// 좋아요 버튼 컴포넌트 정의 (클래스형 컴포넌트)
class LikeButton extends React.Component {
    // 생성자: 컴포넌트가 처음 생성될 떄 실행됨
    constructor(props) {
        // 부모 클래스(React.Component)의 생성자를 호출
        super(props);
        // 컴포넌트의 상태(state) 초기값 생성
        this.state = {
            liked: false, // 좋아용가 눌리지 않은 상태
        };
    }
}
