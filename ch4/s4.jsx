// JSX 문법을 사용해 h1 태그 요소를 생성하고, 그 안에 "안녕, 리액트!"라는 텍스트를 넣음
const element = <h1>안녕, 리액트!</h1>;
// HTML 문서에서 id가 "root"인 DOM 요소를 찾아 ReactDOM의 렌더링 루트로 설정
const root = ReactDOM.createRoot(document.getElementById("root"));
// 앞에서 만든 element(React 요소)를 root에 렌더링하여 화면에 표시
root.render(element);

// r1
// JSX 문법을 사용해 h1 태그 요소를 생성하고, 그 안에 텍스트를 넣음
const element1 = <h1>안녕, 리액트!</h1>;
// HTML 문서에서 id가 "root"인 DOM요소를 찾아 ReactDOM의 렌더링 루트로 설정
const root1 = ReactDOM.createRoot(document.getElementById("root"));
// 앞에서 만든 element1(React 요소)를 root1에 렌더링하여 화면에 표시
root1.render(element1);

// r2
// JSX 문법을 사용해 h1 태그 요소를 생성하고, 그 안에 텍스트를 넣음
const element2 = <h1>안녕, 리액트!</h1>;
// HTML문서에서 id가 'root'인 DOM요소를 찾아 ReactDOM의 렌더링 루트로 설정
const root2 = ReactDOM.createRoot(document.getElementById("root"));
// 앞에서 만든 element2(React요소)를 root2에 렌더링하여 화면에 표시
root2.render(element2);

// r3
// JSX 문법을 사용해 h1 태그 요소를 생성하고, 그 안에 텍스트를 넣음
const element3 = <h1>안녕, 리액트!</h1>;
// HTML 문서에서 id가 'root'인 DOM요소를 찾아 ReactDOM의 렌더링 루트로 설정
const root3 = ReactDOM.createRoot(document.getElementById("root"));
// 앞에서 만든 element3(React 요소)를 root3에 렌더링하여 화면에 표시
root3.render(element3);

// r4
// JSX 문법을 사용해 h1 태그 요소를 생성하고, 그 안에 텍스트를 넣음
const element4 = <h1>안녕, 리액트!</h1>;
// HTML 문서에서 id가 'root'인 DOM요소를 찾아 ReactDOM의 렌더링 루트로 설정
const root4 = ReactDOM.createRoot(document.getElementById("root"));
// 앞에서 만든 element4(React요소)를 root4에 렌더링하여 화면에 표시
root4.render(element4);

// r5
// JSX 문법을 사용해 h1 태그 요소를 생성하고, 그 안에 텍스트를 넣음
const element5 = <h1>안녕, 리액트!</h1>;
// HTML 문서에서 id가 'root'인 DOM요소를 찾아 ReactDOM의 렌더링 루트로 설정
const root5 = ReactDOM.createRoot(document.getElementById("root"));
// 앞에서 만든 element5(React요소)를 root5에 렌더링하여 화면에 표시
