// ❌ state를 직접 수정 (잘못된 사용법)
// React는 state 변경을 감지하지 못해 화면이 갱신되지 않을 수 있음
this.state = {
    name: "Inje",
};
// ✅ setState 함수를 통한 수정 (올바른 사용법)
// state 변경을 React에게 알리고, 화면을 다시 렌더링함
this.setState({
    name: "Inje",
});

// r1
// state를 직접 수정 (잘못된 사용법)
// React는 state 변경을 감지하지 못해 화면이 갱신되지 않을 수 있음
this.state = {
    name: "inje",
};
// setState 함수를 통한 수정 (정상적인 사용법)
// state 변경을 React에게 알리고, 화면을 다시 렌더링
this.setState({
    name: "Inje",
});

// r2
// state를 직접 수정 (잘못된 사용법)
// React는 state 변경을 감지하지 못해 화면이 갱신되지 않을 수 있음
this.state = {
    name: "inje",
};
// setState 함수를 통한 수정 (정상적인 사용법)
// state 변경을 React에게 알리고, 화면을 다시 렌더링
this.setState({
    name: "inje",
});

// r3
// state를 직접 수정 (잘못된 사용법)
// React는 state 변경을 감지하지 못해 화면이 갱신되지 않을 수 있음
this.state = {
    name: "inje",
};
// setState 함수를 통한 수정 (정상적인 사용법)
// state 변경을 React에게 알리고, 화면을 다시 렌더링
this.setState({
    name: "inje",
});

// r4
// state를 직접 수정 (잘못된 사용법)
// React는 state 변경을 감지하지 못해 화면이 갱신되지 않을 수 있음
this.state = {
    name: "inje",
};
// setState 함수를 통한 수정 (정상적인 사용법)
// state 변경을 React에게 알리고, 화면을 다시 렌더링
this.setState({
    name: "inje",
});

// r5
// state를 직접 수정 (잘못된 사용법)
// React는 state 변경을 감지하지 못해 화면이 갱신되지 않을 수 있음
this.state = {
    name: "inje",
};
// setState 함수를 통한 수정 (정상적인 사용법)
// state 변경을 React에게 알리고, 화면을 다시 렌더링
this.setState({
    name: "inje",
});
