// 컴포넌트 이름은 항상 대문자로 시작해야 한다.
// 리액트는 소문자로 시작하는 태그를 HTML 기본 DOM 태그로 인식한다.
// 소문자 div → HTML div 태그로 인식
const element = <div />;
// 대문자로 시작하는 Welcome → 사용자 정의 리액트 컴포넌트로 인식
// name="인제" 는 Welcome 컴포넌트에 props로 전달된다
const element1 = <Welcome name="인제" />;

// r1
/* 컴포넌트 이름은 항상 대문자로 시작해야 한다.
리액트는 소문자로 시작하는 태그를 HTML 기본 DOM 태그로 인식한다. */
// 소문자 div -> HTML div 태그로 인식
const element2 = <div />;
// 대문자로 시작하는 Welcome -> 사용자 정의 리액트 컴포넌트로 인식
// name="인제"는 Welcome 컴포넌트에 props로 전달된다.
const element3 = <Welcome name="인제" />;

// r2
// 컴포넌트 이름은 항상 대문자로 시작해야 한다.
// 리액트는 소문자로 시작하는 태그를 HTML 기본 DOM 태그로 인식한다.
// 소문자 div -> HTML div 태그로 인식
const element4 = <div />;
// 대문자로 시작하는 Welcome -> 사용자 정의 리액트 컴포넌트로 인식
// name="인제"는 Welcome 컴포넌트에 props로 전달된다.
const element5 = <Welcome name="인제" />;

// r3
// 컴포넌트 이름은 항상 대문자로 시작해야 한다.
// 리액트는 소문자로 시작하는 태그를 HTML 기본 DOM 태그로 인식한다.
// 소문자 div -> HTML div 태그로 인식
const element6 = <div />;
// 대문자로 시작하는 Welcome -> 사용자 정의 리액트 컴포넌트로 인식
// name="인제"는 Welcome 컴포넌트에 props로 전달된다.
const element7 = <Welcome name="인제" />;

// r4
// 컴포넌트 이름은 항상 대문자로 시작해야 한다.
// 리액트는 소문자로 시작하는 태그를 HTML 기본 DOM 태그로 인식한다.
// 소문자 div -> HTML div 태그로 인식
const element8 = <div />;
// 대문자로 시작하는 Welcome -> 사용자 정의 리액트 컴포넌트로 인식
// name="인제"는 Welcome 컴포넌트에 props로 전달된다.
const element9 = <Welcome name="인제" />;

// r5
// 컴포넌트 이름은 항상 대문자로 시작해야 한다.
// 리액트는 소문자로 시작하는 태그를 HTML 기본 DOM 태그로 인식한다.
// 소문자 div -> HTML div 태그로 인식
const element10 = <div />;
// 대문자로 시작하는 Welcome -> 사용자 정의 리액트 컴포넌트로 인식
// name="인제"는 Welcome 컴포넌트에 props로 전달된다.
const element11 = <Welcome name="인제" />;
