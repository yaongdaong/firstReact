// 함수(function):입력을 받아서 정해진 출력을 하는 것
// function statement 사용
function sum(a, b) {
    return a + b;
}
console.log(sum(10, 20));

// arrow function expression 사용
const multiply = (a, b) => {
    return a * b;
};
console.log(multiply(10, 20));
