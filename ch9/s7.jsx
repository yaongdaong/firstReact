// props.warning값이 true일 경우에만 경고메시지 출력, false면 아무것도 출력안함
function WarningBanner(props) {
    if (!props.warning) {
        return null;
    }
    return <div>경고!</div>;
}

// r1
// props.warning값이 true일 경우에만 경고메시지 출력, false면 아무것도 출력안함
// 특정 컴포넌트를 렌더링하고 싶지 않으면 null을 리턴하면 된다
function WarningBanner(props) {
    if (!props.warning) {
        return null;
    }
    return <div>경고!</div>;
}

// r2
// props.warning값이 true일 경우에만 경고메시지 출력, false면 아무것도 출력안함
// 특정 컴포넌트를 렌더링하고 싶지 않으면 null을 리턴하면 된다
function WarningBanner(props) {
    if (!props.warning) {
        return null;
    }
    return <div>경고!</div>;
}

// r3
// props.warning값이 true일 경우에만 경고메시지 출력, false면 아무것도 출력안함
// 특정 컴포넌트를 렌더링하고 싶지 않으면 null을 리턴하면 된다
function WarningBanner(props) {
    if (!props.warning) {
        return null;
    }
    return <div>경고!</div>;
}

// r4
// props.warning값이 true일 경우에만 경고메시지 출력, false면 아무것도 출력안함
// 특정 컴포넌트를 렌더링하고 싶지 않을 경우 null을 리턴하면 된다
function WarningBanner(props) {
    if (!props.warning) {
        return null;
    }
    return <div>경고!</div>;
}

// r5
// props.warning값이 true일 경우에만 경고메시지 출력, false면 아무것도 출력안함
// 특정 컴포넌트를 렌더링하고 싶지 않으면 null을 리턴하면 된다.
function WarningBanner(props) {
    if (!props.warning) {
        return null;
    }
    return <div>경고!</div>;
}
