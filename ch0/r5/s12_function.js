// 함수
// fuction statement를 사용
function sum(a, b) {
    return a + b;
}
console.log(sum(10, 20));
// arrow funcion expression 사용
const multiply = (a, b) => {
    return a * b;
};
console.log(multiply(10, 20));
